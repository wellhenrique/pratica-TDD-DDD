type RecordProps = {
  name: string;
  age: number;
};

export class Record {
  execute(props: RecordProps): void {}
  // name: string;
  // age: number;
  // course: string;
  // media: number;
  // document: string;
  // status: string;
}
